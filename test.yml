---
- hosts: localhost
  gather_facts: true
  become: false
  vars:
    - test1: i have set this
  tasks:
    - name: test varible output
      debug:
        msg: "test varible is it set? {{ test1 }} "
